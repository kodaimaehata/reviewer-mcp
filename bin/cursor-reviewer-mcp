#!/usr/bin/env node
// Launch built MCP server (stdio)
import('../dist/server.js').catch((err) => {
  console.error('[cursor-reviewer-mcp] Build not found. Run: npm run build');
  console.error(err);
  process.exit(1);
});

